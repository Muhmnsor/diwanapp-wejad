import {
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";

export const EditProjectActivityHeader = () => {
  return (
    <DialogHeader className="text-right">
      <DialogTitle className="text-right">تعديل نشاط المشروع</DialogTitle>
    </DialogHeader>
  );
};