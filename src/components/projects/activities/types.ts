
export interface ProjectActivityFormData {
  title: string;
  description: string;
  date: string;
  time: string;
  activity_duration: number;
  location: string;
  location_url?: string;
  special_requirements?: string;
}
